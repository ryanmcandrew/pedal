#:kivy 1.0

# delay_seconds, feedback, mix

<DelayWidget>:

    # BoxLayout:
        # orientation:

    Label:
        text: 'Delay - '
        

    BoxLayout:
         
        # GridLayout:
        # BoxLayout:
                # Label:
                    # text: 'test'
            # Column:
            # RowLayout:
        rows: 2
        cols:1
        width: 100
        height: 100

        # cols: 1
        Slider:
            id: delay_seconds_slider
            min: 0
            max: 1
            step: .01
            value: 0.5
            orientation: 'vertical'
            # on_touch_move: root.update_delay(delay_seconds_slider.value, feedback_slider.value, mix_slider.value)
            # on_touch_move: root.delay_seconds = delay_seconds_slider.value
    # Row:
        Label:
            text: 'delay_seconds - ' + str(round(delay_seconds_slider.value, 2))
    BoxLayout:
        
        # GridLayout:
        # rows:2
        # cols:1
        # height:100
        Slider:
            id: feedback_slider
            min: 0.0
            max: 1.0
            step: 0.01
            value: 0.03
            orientation: 'vertical'
            # on_touch_move: root.feedback = feedback_slider.value
        Label:
            text: 'feedback - ' + str(round(feedback_slider.value, 2))
    # # GridLayout:
    BoxLayout:
        Slider:
            id: mix_slider
            min: 0
            max: 1
            step: .01
            value: 0.5
            orientation: 'vertical'
            # on_touch_move: root.update_delay(delay_seconds_slider.value, feedback_slider.value, mix_slider.value)
        Label:
            text: 'mix - ' + str(round(mix_slider.value, 1))
